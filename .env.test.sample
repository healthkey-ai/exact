# Test environment — no external DB URLs so all queries use the local test DB.
SECRET_KEY=test-secret-key
DEBUG=true
DATABASE_NAME=exact
DATABASE_USER=exact
DATABASE_PASSWORD=
DATABASE_HOST=localhost
DATABASE_PORT=5432
